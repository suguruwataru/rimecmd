[package]
name = "rimecmd"
version = "0.1.0"
edition = "2021"
description = "Use Rime via the command line."

[dependencies]
clap = { version = "4.4.11", features = ["derive"] }
libc = "0.2.150"
xdg = "2.5.2"

[dev-dependencies]
rand = "0.8.5"
